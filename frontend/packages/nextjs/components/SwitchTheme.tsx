// Theme switcher disabled - RadiShield uses light theme only for professional appearance

export const SwitchTheme = ({ className }: { className?: string }) => {
  // Return null to hide theme switcher completely
  return null;
};
